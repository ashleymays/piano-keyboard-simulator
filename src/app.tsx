import { Keyboard } from './components/keyboard';
import { Message } from './components/message';

export const App = () => {
  return (
    <main>
      <Keyboard />
      <Message />
    </main>
  );
};
