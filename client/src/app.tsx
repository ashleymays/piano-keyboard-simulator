import { Keyboard } from './components/keyboard';
import { Toasts } from './components/toasts';

export const App = () => {
  return (
    <main>
      <Keyboard />
      <Toasts />
    </main>
  );
};
