import { Keyboard } from './components/keyboard';
import { Notifications } from './components/notifications';

export const App = () => {
  return (
    <main>
      <Keyboard />
      <Notifications />
    </main>
  );
};
