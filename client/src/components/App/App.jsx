import GlobalContext from '../../contexts/GlobalContext';
import Keyboard from '../Keyboard';

function App() {
    return (
        <GlobalContext>
            <Keyboard />
        </GlobalContext>
    );
}

export default App;
