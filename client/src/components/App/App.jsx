import GlobalContext from '../GlobalContext';
import Keyboard from '../Keyboard';

function App() {
    return (
        <GlobalContext>
            <Keyboard />
        </GlobalContext>
    );
}

export default App;
